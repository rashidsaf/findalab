#!/usr/bin/env bash

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

PROJECT_NAME=findalab

docker-compose -p $PROJECT_NAME \
    -f "$DIR"/../docker/docker-compose.base.yml \
    -f "$DIR"/../docker/docker-compose.test.yml \
    -f "$DIR"/../docker/docker-compose.dev.yml \
    "$@"
