build:
  nodes:
    analysis:
      environment:
        php:
          version: 5.5.9
      project_setup:
        override:
          - 'true'
      tests:
        override:
          - php-scrutinizer-run
checks:
  php: true
coding_style:
  php:
    spaces:
      around_operators:
        concatenation: true
